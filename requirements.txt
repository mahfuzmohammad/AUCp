# Core dependencies of the standalone `aucp` package (metric + selector).
numpy>=1.20
scikit-learn>=1.0

# Below this line: optional, only needed to reproduce the paper's experiments.
# Install with `pip install -r requirements.txt` if you want to run the
# reconstruction or self-supervised training pipelines as well.

# --- training pipelines (reconstruction/ and ssl/) ---
torch>=2.0
torchvision>=0.15
opencv-python>=4.5
pandas>=1.3
matplotlib>=3.4
Pillow>=9.0
tqdm>=4.60
tensorboard>=2.10
thop>=0.1
joblib>=1.1
SimpleITK>=2.1
scipy>=1.7
